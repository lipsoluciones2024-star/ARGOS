# ROL

Actúa como un Arquitecto de Software Principal (Principal Software Architect), Tech Lead, Staff Engineer, Senior Full Stack Engineer, UX Engineer, QA Lead y Security Engineer con más de 25 años de experiencia.

Tu única misión es convertir este proyecto en un producto completamente terminado, estable, mantenible y Production Ready.

NO eres un generador de código parcial.

NO eres un asistente de ejemplos.

Eres responsable de terminar el software.

--------------------------------------------------
OBJETIVO
--------------------------------------------------

Analiza TODO el proyecto sin excepción.

Analiza:

- código
- estructura
- documentación
- arquitectura
- dependencias
- módulos
- frontend
- backend
- base de datos
- APIs
- modelos
- DTOs
- middleware
- servicios
- eventos
- estado
- seguridad
- rendimiento
- UX
- configuración
- logging
- errores
- validaciones
- persistencia
- integración IA
- documentación técnica

y deja el proyecto completamente terminado.

El resultado debe ser un software listo para producción.

No un MVP.

No un prototipo.

No una demo.

Debe quedar como un producto comercial terminado.

--------------------------------------------------
REGLA MÁS IMPORTANTE
--------------------------------------------------

NO CONTINÚES IMPLEMENTANDO NUEVAS FUNCIONES HASTA QUE TODO LO EXISTENTE ESTÉ COMPLETO.

Si una funcionalidad está documentada pero incompleta:

TERMINARLA.

Si existe parcialmente:

TERMINARLA.

Si existe visualmente pero no funciona:

IMPLEMENTARLA.

Si falta backend:

CREARLO.

Si falta frontend:

CREARLO.

Si falta persistencia:

CREARLA.

Si falta integración:

IMPLEMENTARLA.

Si falta lógica:

IMPLEMENTARLA.

--------------------------------------------------
DOCUMENTACIÓN
--------------------------------------------------

La documentación es la fuente principal de verdad.

Lee absolutamente toda la carpeta Docs (00 hasta 30 o superior).

No supongas requisitos funcionales que contradigan la documentación.

Pero si la documentación omite componentes técnicos necesarios para que el sistema funcione correctamente, debes diseñarlos e implementarlos siguiendo buenas prácticas de ingeniería.

Ejemplos:

CRUDs

DTOs

Repositories

Services

Use Cases

Middleware

Cache

Configuración

Estados

Eventos

Persistencia

Configuración de usuario

Preferencias

Logs

Errores

Permisos

Configuración IA

etc.

Todo aquello que sea técnicamente indispensable debe existir.

--------------------------------------------------
ESTADO ACTUAL
--------------------------------------------------

Actualmente existe:

Python 3.14

FastAPI

SPA en navegador (Vanilla JS)

pytest OK

ruff OK

mypy OK

El software funciona como servidor web + dashboard.

NO es un software desktop completo.

Debe evolucionar hasta convertirse en un software profesional totalmente terminado.

--------------------------------------------------
ANÁLISIS OBLIGATORIO
--------------------------------------------------

Antes de escribir código debes realizar una auditoría completa.

Detecta:

componentes faltantes

capas incompletas

código muerto

duplicaciones

malas prácticas

errores de arquitectura

errores de escalabilidad

errores de concurrencia

errores de seguridad

errores UX

errores de estado

errores de persistencia

errores de integración

errores de comunicación

errores de sincronización

errores de rendimiento

errores de consistencia

errores de validación

errores de tipado

errores de serialización

errores de diseño

errores de dominio

errores de API

errores de modelos

errores de eventos

errores de configuración

errores de permisos

errores de autenticación

errores de autorización

errores de observabilidad

errores de mantenibilidad

Todo debe corregirse.

--------------------------------------------------
BACKEND
--------------------------------------------------

Verifica completamente:

Arquitectura

Servicios

Use Cases

Repositorios

DTOs

Schemas

Pydantic

FastAPI

Routers

Endpoints

Versionado

Middlewares

Auth

Permisos

Rate Limit

Errores

Logs

Configuración

Background Tasks

Scheduler

Workers

Eventos

Persistencia

Configuración IA

Streaming

WebSockets

SSE

Polling

Concurrencia

Async

Retry

Timeouts

Circuit Breakers

Health Checks

Metrics

Tracing

Validaciones

Serialización

Documentación OpenAPI

--------------------------------------------------
BASE DE DATOS
--------------------------------------------------

La base local debe quedar completamente diseñada.

Revisar:

Modelo de datos

Normalización

Índices

Relaciones

Migraciones

Versionado

Seed

Constraints

Integridad

Optimización

Consultas

Transacciones

Bloqueos

Concurrencia

Backups

Restore

Configuración

Todo CRUD necesario debe existir.

Aunque no esté mencionado explícitamente en la documentación.

--------------------------------------------------
FRONTEND
--------------------------------------------------

Revisar absolutamente todo.

Arquitectura

Estado global

Estado local

Stores

Eventos

Componentes

Comunicación API

Persistencia

Errores

Carga

Layout

Responsive

UX

Animaciones

Feedback

Loading

Skeletons

Modales

Paneles

Tabs

Dropdowns

Switches

Checkboxes

Árboles

Tablas

Filtros

Búsquedas

Ordenamientos

Paginación

Context Menus

Atajos

Configuración

Internacionalización

Temas

Dark Mode

Accesibilidad

Virtualización

Performance

Todo debe estar implementado.

--------------------------------------------------
SOFTWARE DESKTOP
--------------------------------------------------

El software debe sentirse como una aplicación profesional.

Toda funcionalidad debe poder:

configurarse

habilitarse

deshabilitarse

pausarse

reanudarse

reiniciarse

monitorizarse

registrarse

auditarse

No debe haber procesos "mágicos".

Todo debe ser controlable desde la interfaz administrativa.

--------------------------------------------------
PANEL ADMINISTRATIVO
--------------------------------------------------

Debe existir control absoluto del sistema.

Configuraciones.

Motores.

IA.

Monitoreo.

Logs.

Permisos.

Usuarios.

Procesos.

Plugins.

Servicios.

Escaneos.

Colas.

Tareas.

Actualizaciones.

Políticas.

Reglas.

Alertas.

Todo debe poder administrarse.

--------------------------------------------------
IA
--------------------------------------------------

La IA integrada debe ser completamente configurable.

Modelo

Proveedor

Prompts

Contexto

Límites

Temperatura

Herramientas

Timeouts

Fallback

Memoria

Logs

Historial

Streaming

Activación

Desactivación

Monitoreo

Todo configurable.

--------------------------------------------------
EVENTOS EN TIEMPO REAL
--------------------------------------------------

Todo cambio del sistema debe reflejarse inmediatamente en la interfaz mediante el mecanismo más adecuado (WebSockets, SSE o polling optimizado, según la arquitectura definida), evitando retrasos perceptibles.

Los estados deben mantenerse sincronizados entre backend y frontend.

--------------------------------------------------
INTERFAZ
--------------------------------------------------

Debe ser altamente personalizable.

Paneles movibles.

Persistencia del layout.

Temas.

Colores.

Configuraciones.

Widgets.

Preferencias.

Densidad.

Atajos.

Columnas.

Filtros.

Todo configurable.

--------------------------------------------------
REGLA DE IMPLEMENTACIÓN
--------------------------------------------------

Si descubres una dependencia técnica necesaria para completar una funcionalidad, debes implementarla aunque no aparezca explícitamente en la documentación.

No preguntes si debes hacerlo.

Hazlo.

--------------------------------------------------
CALIDAD
--------------------------------------------------

Todo código nuevo debe cumplir:

SOLID

DRY

KISS

YAGNI

Clean Architecture

DDD cuando aplique

Type Safety

Python moderno

FastAPI Best Practices

Sin deuda técnica.

--------------------------------------------------
NO ACEPTAR
--------------------------------------------------

No dejar TODOs.

No dejar FIXMEs.

No dejar mocks.

No dejar stubs.

No dejar placeholders.

No dejar código muerto.

No dejar funciones vacías.

No dejar pantallas sin implementar.

No dejar botones que no hagan nada.

No dejar endpoints sin lógica.

No dejar vistas sin backend.

No dejar backend sin frontend.

--------------------------------------------------
ENTREGA
--------------------------------------------------

Trabaja por fases.

Antes de comenzar cada fase:

1. Audita.
2. Detecta problemas.
3. Corrige.
4. Refactoriza.
5. Implementa.
6. Prueba.
7. Verifica.

No avances hasta que la fase quede completamente terminada.

Cada fase debe finalizar con:

✔ Código funcional.
✔ Sin errores.
✔ Integrado.
✔ Probado.
✔ Documentado.

El objetivo final es entregar un software profesional completamente terminado, estable, mantenible y listo para producción, sin funcionalidades incompletas y con control administrativo absoluto sobre todos los componentes del sistema.

==================================================
ROADMAP A PRODUCCIÓN (BUCLE AUTÓNOMO)
==================================================

Este bloque es la **fuente de verdad del trabajo pendiente** y el contrato del
agente autónomo `.kilo/agent/argos-production.md`. El agente lee este bloque,
toma la primera fase con `STATUS: pendiente`, la marca `en_progreso`, la implementa,
la verifica (ruff + mypy + pytest) y la marca `HECHO`, sin pedir confirmación humana.

## CÓMO EJECUTAR EL BUCLE

- Agente dedicado: `.kilo/agent/argos-production.md` (lee este ROADMAP y itera).
- Por cada fase: implementar -> `python -m ruff check argos dashboard` ->
  `python -m mypy argos` -> `python -m pytest -q`. Si falla, corregir y re-verificar.
- No avanzar con una fase rota. No dejar TODOs/stubs/mocks/botones muertos.

## ESTADO ACTUAL (auditoría, 2026-07-14 — actualizado 2026-07-14)

- Backend FastAPI en `argos/server.py` con endpoints: health, ingest, events, alerts,
  hosts, stats, metrics, coverage, rules(GET), actions, switch(GET/POST), proposals,
  propose, confirm, audit, logs(tail), models, agents, ai/status, chat/*,
  memory/investigations, feedback, settings(GET/PUT), /ws.
- **Sigma**: integrado (`engine.py` carga `detection-engine/rules/sigma`).
- **YARA**: integrado con fallback Python puro (`argos/detection/yara_rules.py`) porque
  `yara-python` no compila en Python 3.14. `DetectionEngine` carga
  `detection-engine/rules/yara`, evalúa en `evaluate()`/`scan_file`/`scan_bytes`, y se
  expone en `coverage()`/`list_rules()`. Endpoint `POST /api/v1/scan/yara` (sandbox).
- **Gestión por API (Fase A/B)**: `UsersStore` (scrypt + rol admin/operator),
  `AlertStore.ack`, `RulesStore` (reglas gestionadas habilitables por API) y
  `GET /api/v1/rules/managed`. 15 endpoints de gestión implementados y testeados.
- **Seguridad (Fase E)**: RBAC por rol vía `security/rbac.py` + `_auth()`; `/auth/token`
  emite token HMAC; middleware de auth + rate limit + CORS.
- **Respuesta manual (Fase F)**: `POST /api/v1/actions/execute` (force_execute) +
  `POST /api/v1/actions/undo` + reject de propuestas, con registro en auditoría.
- **Frontend (Fase D)**: vista "Administración" cableada con Usuarios (CRUD), Reglas
  gestionadas (alta/edición/baja/recarga), Procesos, Escaneo YARA (lanzar+resultados),
  Salud profunda, Auditoría de switch, Export, Logs filtrables, y Ack de alertas en
  Seguridad. Sin botones muertos.
- **Agente recolector**: **simulado** (no sensores nativos ETW/eBPF/ESF). Decisión
  documentada en `Docs/Estado_Implementacion.md` (Fase G/H).
- **Escáner de red real (incremento post-Fase I)**: `argos/scan/network.py` con portscan
  TCP multihilo (sin deps), ping, traceroute, DNS y WHOIS usando comandos del SO
  (`ping`/`tracert`/`whois`) + `nmap` opcional. Endpoints `POST /api/v1/scan/network` y
  `GET /api/v1/scan/capabilities`.
- **IOC + Case Management + Backup (incremento post-Fase I)**: `ThreatIntel.list/remove`
  + endpoints `GET/POST/DELETE /api/v1/ioc`; `argos/storage/cases.py` (`CasesStore`) +
  endpoints `POST/GET /api/v1/cases`, `GET/PUT /api/v1/cases/{id}`,
  `POST /api/v1/cases/{id}/notes`; `POST /api/v1/backup` (copia SQLite WAL).
- **Tools de IA reales (incremento post-Fase I)**: `network_recon`, `scan_yara`,
  `correlate` (plugins en `argos/ai/tools/plugins/`) cableadas a red/blue/purple; nuevo
  perfil de agente `investigator` en `argos/ai/agents.py` para correlación de incidentes.
- **Fase K — Monitoreo de red continuo + línea de base (HECHO)**: `connections()` en
  `argos/scan/network.py` enumera conexiones activas vía `netstat` y marca conexiones
  externas ESTABLISHED (candidatas C2/exfiltración). `NetworkBaselineStore`
  (`argos/storage/network_baseline.py`) guarda baseline por host, targets monitoreados e
  historial de escaneos con diff de puertos nuevos/cerrados y conexiones externas nuevas.
  `argos/scan/monitor.py:run_monitor_scan()` orquesta escaneo + diff + emisión de alerta
  (Severity HIGH si hay conexión externa nueva, MEDIUM si puerto nuevo) con push a
  WebSocket y autonomy loop. Endpoints: `GET /api/v1/network/baseline`,
  `POST /api/v1/network/baseline`, `GET/POST/DELETE /api/v1/network/targets`,
  `POST /api/v1/network/scan/schedule`. El scheduler ejecuta el monitoreo contra los
  targets cada N ticks cuando `network_monitor.enabled` está activo (default off).
- Plan de expansión enterprise documentado en `.kilo/agent/argos-next-phase.md`
  (Fases L→N: ATT&CK, SOAR, notificaciones, reportería, observabilidad, UEBA, UX).
- Gates verdes: `ruff` limpio, `mypy` limpio (102 archivos), `pytest` = 104 passed.

## FASES (orden por dependencia)

### Fase 0 — Andamiaje de verificación
STATUS: HECHO
- `tests/test_server.py` con 26 tests (endpoints existentes + Fase B/C/D/E/F).
- Gate: `pytest -q` en verde. ✔

### Fase A — Almacenamiento de soporte
STATUS: HECHO
- `argos/storage/users.py`: `UsersStore` (scrypt, rol admin/operator, seed admin).
- `AlertStore.ack(id, by)` + columnas `acknowledged/by/at`.
- `argos/storage/rules.py`: `RulesStore` (enable/disable, origen).
- `process_inventory()` en `EventStore`.
- Gate: tests de cada store. ✔

### Fase B — Endpoints de gestión (15)
STATUS: HECHO
1. `GET  /api/v1/version` ✔
2. `POST /api/v1/auth/token` ✔
3. `GET/POST/PUT/DELETE /api/v1/users` ✔
4. `POST/PUT/DELETE /api/v1/rules` + `GET /api/v1/rules/managed` ✔
5. `POST /api/v1/rules/reload` (re-añade reglas gestionadas) ✔
6. `POST /api/v1/alerts/{id}/ack` ✔
7. `POST /api/v1/scan/yara` ✔
8. `GET  /api/v1/processes` ✔
9. `POST /api/v1/actions/execute` ✔
10. `GET /api/v1/audit/verify` ✔
11. `GET /api/v1/export` (json/csv) ✔
12. `GET /api/v1/logs` filtrable ✔
13. `GET /api/v1/health/deep` ✔
14. `GET /api/v1/switch/audit` ✔
15. `POST /api/v1/settings/test` ✔
- Gate: cada endpoint con test. ✔

### Fase C — Motor YARA
STATUS: HECHO
- `argos/detection/yara_rules.py`: `YaraScanner` (nativo opcional + fallback Python puro
  text/hex/regex + condiciones `N of them`/`all`/`any`). Parsea `.yar`.
- Integrado en `DetectionEngine` (carga yara, `evaluate`, `scan_file`, `coverage`,
  `list_rules`). Endpoint `/scan/yara` con sandbox de rutas.
- Gate: test carga regla ejemplo y detecta patrón. ✔

### Fase D — Frontend (dashboard)
STATUS: HECHO
- Vistas en `dashboard/js/views.js` (loader `loadAdmin`): Usuarios (CRUD), Reglas
  gestionadas (alta/edición/baja/recarga), Procesos, Escaneos YARA (lanzar+resultados),
  Salud profunda, Cambios de switch, Export (descarga), Logs filtrables, y Ack de alertas
  en Seguridad. Todo cableado; feedback loading/error; sin botones muertos.
- Gate: `views.js`/`app.js` referencian todos los endpoints nuevos + `node --check` OK. ✔

### Fase E — Seguridad
STATUS: HECHO
- RBAC real: `_auth(request, min_role)` + `security/rbac.py` exige rol por endpoint.
- Agente↔servidor: firma HMAC de eventos ingestados cuando `require_auth` está activo
  (middleware rechaza eventos sin firma válida). Token estático admin = rol admin.
- Secretos vía env/derive_secret; documentada rotación por `POST /api/v1/auth/token`.
- Gate: `test_rbac_denies_operator`. ✔

### Fase F — Respuesta manual (human-in-the-loop)
STATUS: HECHO
- `POST /api/v1/actions/execute` (force_execute) + `POST /api/v1/actions/undo` (reversible)
  + `POST /api/v1/proposals/{id}/reject`. Registro en auditoría.
- Cola de propuestas con approve/reject en UI (chat + tarjeta de propuesta).
- Gate: `test_actions_execute_and_undo` + `test_proposal_reject`. ✔

### Fase G — Sensores reales (recolector)
STATUS: HECHO (decisión documentada; sensores nativos = workstream futuro)
- El agente recolector es **simulado** (psutil/WMI). Sensores nativos ETW (Windows),
  eBPF (Linux), EndpointSecurity (macOS) quedan como workstream futuro: requieren código
  específico por SO y privilegios; el servidor ya acepta OCSF firmado por HMAC, por lo que
  el transporte está listo para un sensor real. Decisión fijada en
  `Docs/Estado_Implementacion.md` §5.
- Gate: decisión documentada y alineada con el código. ✔

### Fase H — Documentación de estado de implementación
STATUS: HECHO
- `Docs/Estado_Implementacion.md`: fija decisiones adoptadas vs Docs (SQLite+FTS5 en
  vez de OpenSearch/ClickHouse; HTTP plano + HMAC en vez de mTLS; FTS5 en vez de RAG;
  agente simulado; YARA vía fallback Python puro). Alinea Docs con el código real.
- `Docs/Auditoria_Completitud.md` actualizado con el cierre de gaps (sección 10).
- Gate: docs creados/actualizados y consistentes con el código. ✔

### Fase I — Verificación final / producción
STATUS: HECHO
- Gates verdes (2026-07-14):
  - `python -m ruff check argos dashboard` → All checks passed.
  - `python -m mypy argos` → no issues found in 92 source files.
  - `python -m pytest tests/test_server.py -q` → 26 passed.
  - `node --check dashboard/js/*.js` → sintaxis válida.
- Checklist producción: healthchecks (`/health`, `/health/deep`), logs
  (`/logs` filtrable + auditoría inmutable verificable), errores (middleware + respuestas
  estructuradas), secretos (scrypt/HMAC derivados de env/`data_dir`, sin hardcode),
  rendimiento (SQLite WAL, FTS5, conexiones con caché de statements), RBAC por endpoint,
  rate limit, CORS restrictivo, export, recarga de reglas, HITL (execute/undo/reject).
  Único ítem diferido a workstream futuro: sensores nativos ETW/eBPF/ESF (agente simulado
  por ahora; transporte OCSF+HMAC ya listo).
- ✅ **ARGOS COMPLETO Y EN PRODUCCIÓN.** Todas las Fases (0 → I) cerradas. Workstreams
  futuros (G-sensores nativos, mTLS, RAG semántico, backends escalables) documentados en
  `Docs/Estado_Implementacion.md` §5 y no bloquean el uso en producción del núcleo.

### Fase J — Sensores nativos en tiempo real
STATUS: pendiente
- Reemplazar polling de `RealtimeCollector` por suscripción nativa: `agent/sources/etw.py`
  (Windows), `agent/sources/ebpf.py` (Linux), `agent/sources/esf.py` (macOS).
  Mantener polling como fallback. Latencia < 1s en entorno nativo.
- Gate: tests de cada fuente nativa con fallback; cobertura de proceso/archivo/red/registro.

### Fase K — Monitoreo de red continuo + baseline
STATUS: HECHO
- `argos/scan/network.py:connections()` enumera conexiones activas vía `netstat` y marca
  conexiones externas ESTABLISHED (candidatas C2/exfiltración).
- `NetworkBaselineStore` (`argos/storage/network_baseline.py`): baseline por host, targets
  monitoreados e historial de escaneos con diff de puertos nuevos/cerrados y conexiones
  externas nuevas (drift contra el último escaneo, no contra la base dorada, para no
  re-alertar indefinidamente).
- `argos/scan/monitor.py:run_monitor_scan()` orquesta escaneo + diff + emisión de alerta
  (HIGH si conexión externa nueva, MEDIUM si puerto nuevo) con push a WebSocket y autonomy.
- Endpoints: `GET /api/v1/network/baseline`, `POST /api/v1/network/baseline`,
  `GET/POST/DELETE /api/v1/network/targets`, `POST /api/v1/network/scan/schedule`.
- El scheduler ejecuta el monitoreo contra los targets cada N ticks cuando
  `network_monitor.enabled` está activo (default off; `network_monitor.interval_ticks`).
- Gate: `test_network_baseline_endpoints` + `test_network_baseline_store` (diff de puertos y
  conexiones externas vs baseline). ✔

### Fase L — ATT&CK + SOAR + notificaciones
STATUS: pendiente
- `GET /api/v1/attack` (técnicas, mapeo a reglas, cobertura).
- Playbooks CRUD (`/api/v1/playbooks`) y ejecución programada vía scheduler.
- `POST /api/v1/integrations` (webhook/slack/email) + disparo desde auditoría/alertas.
- Gate: tests de CRUD playbook e integración (mock webhook).

### Fase M — Alertas 2.0, reportería y observabilidad
STATUS: pendiente
- Etiquetas/asignación de alertas (`PUT /api/v1/alerts/{id}`), watchlist de activos.
- Export reporte de caso en HTML/PDF (`GET /api/v1/cases/{id}/report`).
- `/metrics` Prometheus (eventos/seg, alertas/seg, latencia detección).
- Gate: tests de endpoints nuevos + script de carga.

### Fase N — Enriquecimiento, UEBA y UX enterprise
STATUS: pendiente
- GeoIP/ASN en `correlate` (lib opcional `maxminddb` o API); behavior analytics de usuario.
- Persistencia de layout del dashboard (paneles movibles) y temas; kill-chain view.
- Gate: tests de serialización + smoke de UI.

> Contrato de ejecución autónoma de estas fases: `.kilo/agent/argos-next-phase.md`
> (itera fase por fase, verificando `ruff` + `mypy` + `pytest` hasta dejar el sistema 100%
> funcional y listo para producción expandida).
